

type check like:
	cd backend
	mypy __init__.py --follow-imports=silent


resample at ~5m intervals when uploading gps. faster to upload.
	known spacing for finding chainages?


georeferencing with .exe instead of .bat. probabaly faster. closer to stand alone app.


web app like:
rest api and database handles fitting & corrections.
creates georeferenced images on server/in database.
display to user on page. Geoserver?


georeferenceRun(runPk:int)
should show cancellable progress dialog and block ui.
could use subprocess and program in any language that displays this



save as json? {run:startFrame,endFrame,corrections:[[]],'images':[[]]}
easier to just save sqlite database. could have version problems loading files made by old versions of plugin.




x,y corrections vs m,offset:

	simpler to display and explain to users		
	don't need to change start/end frames
	chainages seem to be leading/lagging correct position. 

	cant go past start/end of gps


	use frame,pixel,line -> xy with
	single chainage shift (minimize distances) and x,y shift?

	correctedPoint(m,offset) = point(m + chainageShift(run),offset) + XYShift(m)
	
	

actions:
	process:
	{
	georeference
	remake VRT
	load VRT
	}
	
	load GPS
	load Cracking
	load rutting
	load joint faulting
	
	
	
	
	

concurrency in batch file:
	use start to run smallish number of commands concurrently and await completion.
	do this in sequence.
	
	too small results in CPU idling whilst waiting for other commands to complete. too large in crash?.
	
	much simpler than process pool. inefficient if some processes take much longer than others
	
	
	
stand alone:

	c++ and QT for static typing and no version/depency problems.
	
	QGraphics scene and QPixmap to show images?	avoids file locks. efficiency? avoids need for qgis at all?
	could load warped image.
	or do in memory? gdal c++ bindings? or QPixmap.setRotation() and setScale()
		
	
	QgsMapCanvas is QGraphicsView subclass
	
	
	
	
gps/odometer errors;
	wheel diameter ~2%
	
	speedometer:~5%
	
	corners make gps distance smaller than it should be.
	
	typical gps = 2-5m
	
	does it use moving average of position?
	
	
	

	


stand alone:

	split into runs.	
		display centerline
	
	
	georeference
	
	corrections
	
		need to display current image location and background map.

	
	could edit distress layers in QGIS. need way to import and correct acdx
	
	
	
	
ravelling
	
	
