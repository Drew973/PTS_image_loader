

view depressions


database vs in memory?

	potential for shared database/collaboration
	subprocess can read database. multiple languages.
	database size ~20-50mb. should be fine in memory.	
	
	

single model for mfv,run,corrections,images?


save and load sqlite database?
	
seperate file per mfv number.
sqlite?
need to save and load this within few seconds.



don't have easy way to seperate concerns, eg georeferencing depends on gps and corrections and images.

better to use functions that take database as input? store all states here.

	
	
correction maps frame,pixel,line to x,y.
single chainage shift/run that minimises distance then interpolated x,y shift.

good enough corrections could avoid need to split?
	
	
single chainage,offset shift per run to frame,pixel,line:
	first frame,line 0, pixel:PIXELS/2 to x,y of m(first_frame)+chainage_shift,offset

	
	
	
try to avoid spatial in database at all. less spatialite wierdness.
	

clear GPS when srid changed. warning message.

functions for database CRUD with model or in database_functions.py?
	all database specific functions in single file would make changing to something other than sqlite easier.




may want treeview. 
add type hints whilst redoing.

qcombobox for mfv number
qtableview for runs. use setRootIndex when MFV number in qcombobox changed.
qtableview for corrections. setRootIndex when run in run qcombobox changed. changing MFV combobox should set model for run combobox.


store spline coefficients in database?
can have many km of gps with many mfv numbers. saves needing to load+recalculate.




georeference with rust?
performance gains? easier multiprocessing.
could just write .exe, run subprocess(modal?) and show terminal.



type check like:
	cd backend
	mypy __init__.py --follow-imports=silent
	mypy db_functions.py --follow-imports=silent

resample at ~5m intervals when uploading gps. faster to upload.
	known spacing for finding chainages?


georeferencing with .exe instead of .bat. probabaly faster. closer to stand alone app.


georeferenceRun(runPk:int)
should show cancellable progress dialog and block ui.
could use subprocess and .exe in any language
	rust + rayon seem promising.
	
 


save as json? {run:startFrame,endFrame,corrections:[[]],'images':[[]]}
easier to just save sqlite database. could have version problems loading files made by old versions of plugin.


x,y corrections vs m,offset:

	simpler to display and explain to users		
	don't need to change start/end frames
	chainages seem to be leading/lagging correct position. 

	cant go past start/end of gps


	use frame,pixel,line -> xy with
	single chainage shift (minimize distances) and x,y shift?

	correctedPoint(m,offset) = point(m + chainageShift(run),offset) + XYShift(m)
	
	

actions:
	process:
	{
	georeference
	remake VRT
	load VRT
	}
	
	load GPS
	load Cracking
	load rutting
	load joint faulting
	
	
	
	
	

concurrency in batch file:
	use start to run smallish number of commands concurrently and await completion.
	do this in sequence.
	
	too small results in CPU idling whilst waiting for other commands to complete. too large in crash?.
	
	much simpler than process pool. inefficient if some processes take much longer than others
	
	
	
stand alone:

	c++ and QT for static typing and no version/depency problems.
	
	QGraphics scene and QPixmap to show images?	avoids file locks. efficiency? avoids need for qgis at all?
	could load warped image.
	or do in memory? gdal c++ bindings? or QPixmap.setRotation() and setScale()
		
	
	QgsMapCanvas is QGraphicsView subclass
	
	
	
	
gps/odometer errors;
	wheel diameter ~2%
	
	speedometer:~5%
	
	corners make gps distance smaller than it should be.
	
	typical gps = 2-5m
	
	does it use moving average of position?
	
	
	

	


stand alone:

	split into runs.	
		display centerline
	
	
	georeference
	
	corrections
	
		need to display current image location and background map.

	
	could edit distress layers in QGIS. need way to import and correct acdx
	





georeference through rust gdal bindings?
	translate and warp in memory.
	read database and use rayon for simpler paralell processing?
	could just open shell 
	print progress with rayon_progress 
	
	need to pass spline in.
	open database in read only?





GPS as 1 layer. MFV number(int) , frame(int)

multiple MFV numbers instead of 1 at a time?

heirarchal data. tree view?

mfv:{
	number : string
	spline: Splinestring
	images: [Image]
	GPS:
	
	runs: [Run]

	Run:{
		start_frame: int
		end_frame: int
	
		public fn process()

		public fn remake_vrt()
		
		public fn load_vrt()
		}
		
		
		corrections : [Correction]

		correction:{
			frame:int
			pixel:int
			line:int
			x:float
			y:float
		}
		
	Image:
		frame: int
		original_file: path
		image_type: enum
		
		public fn load() 
		
		public fn georeference():
			use corrections and spline to calculate GCPs
}
	
	
Correction:
	want to find GCPs per frame that move frame,line,pixel -> x,y
	m_shift per run,
	x_shift and y_shift interpolated from m.



#Python 3.7 for dataclass



finding GCP depends on corrections and GPS. gcp.py that imports corrections and gps.


frame,line,pixel: from click. show marker on map. zoom map. 
x,y: from click. show marker on map. zoom map. 
GPS. corrections. corrected Geom
look into non QGIS option?
use game engine like bevy to display and transform images?
or tauri? 




seperate app for plotting distresses in frame,pixel,line
shows intensity,range, photo(from video) for frame.

save time by removing duplicated areas first.



plot distresses on this. y = m. x = offset.
convert MO -> x,y and output GIS files with x,y distresses.











